cluster:
  allowSchedulingOnControlPlanes: true
  network:
    cni:
      name: custom
  proxy:
    disabled: true
  inlineManifests:
    - name: cilium-bootstrap
      contents: |
        apiVersion: v1
        kind: ServiceAccount
        metadata:
          name: cilium-installer
          namespace: kube-system
        ---
        apiVersion: rbac.authorization.k8s.io/v1
        kind: ClusterRoleBinding
        metadata:
          name: cilium-installer
        roleRef:
          apiGroup: rbac.authorization.k8s.
          kind: ClusterRole
          name: cluster-admin
        subjects:
        - kind: ServiceAccount
          name: cilium-installer
          namespace: kube-system
        ---
        apiVersion: v1
        data:
          values.yaml: |
            # helm install cilium cilium/cilium --version 1.18.0 --namespace kube-system -f cilium/values.yaml
            #
            # Talos requirements for a CNI running as the sole network provider with
            # kube-proxy replaced (see talos/patches/cni-kubeproxy.yaml, which must be
            # applied before this chart is installed):
            #   - k8sServiceHost/k8sServicePort point at KubePrism (127.0.0.1:7445),
            #     which every Talos node exposes locally so Cilium can reach the API
            #     server without an external load balancer.
            #   - cgroup.autoMount is disabled because Talos already mounts the unified
            #     cgroupv2 hierarchy at /sys/fs/cgroup.
            #   - Talos does not allow workloads to load kernel modules, so SYS_MODULE
            #     is dropped from Cilium's default capability set.
            operator:
              replicas: 1
            ipam:
              mode: kubernetes

            kubeProxyReplacement: true

            k8sServiceHost: localhost
            k8sServicePort: 7445

            cgroup:
              autoMount:
                enabled: false
              hostRoot: /sys/fs/cgroup

            securityContext:
              capabilities:
                ciliumAgent:
                  - CHOWN
                  - KILL
                  - NET_ADMIN
                  - NET_RAW
                  - IPC_LOCK
                  - SYS_ADMIN
                  - SYS_RESOURCE
                  - DAC_OVERRIDE
                  - FOWNER
                  - SETGID
                  - SETUID
                cleanCiliumState:
                  - NET_ADMIN
                  - SYS_ADMIN
                  - SYS_RESOURCE
        kind: ConfigMap
        metadata:
          name: cilium-values-hf922bb52h
          namespace: kube-system
        ---
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: cilium-install
          namespace: kube-system
        spec:
          backoffLimit: 3
          template:
            spec:
              containers:
              - args:
                - |
                  set -eu
                  helm repo add cilium https://helm.cilium.io/
                  helm repo update
                  helm upgrade --install cilium cilium/cilium \
                    --version 1.18.0 \
                    --namespace kube-system \
                    -f /values/values.yaml \
                    --wait --timeout 10m
                command:
                - /bin/sh
                - -c
                env:
                - name: KUBECONFIG
                  value: /etc/kubeconfig/kubeconfig
                image: alpine/k8s:1.32.13
                name: cilium-install
                volumeMounts:
                - mountPath: /values
                  name: values
                - mountPath: /etc/kubeconfig
                  name: kubeconfig-in-cluster
                  readOnly: true
              dnsPolicy: Default
              hostNetwork: true
              initContainers:
              - args:
                - |
                  set -eu

                  echo "Checking API server is reachable..."
                  kubectl get --raw=/healthz

                  echo "Checking control-plane node count..."
                  cp_count=$(kubectl get nodes -l node-role.kubernetes.io/control-plane -o name | wc -l)
                  if [ "$cp_count" -ne "$EXPECTED_CONTROL_PLANE_COUNT" ]; then
                    echo "Expected $EXPECTED_CONTROL_PLANE_COUNT control-plane nodes, found $cp_count" >&2
                    exit 1
                  fi

                  echo "Checking kube-proxy is disabled..."
                  if kubectl -n kube-system get daemonset kube-proxy >/dev/null 2>&1; then
                    echo "kube-proxy DaemonSet is still present — talos/patches/cni-kubeproxy.yaml (cluster.proxy.disabled) was not applied to this cluster" >&2
                    exit 1
                  fi

                  echo "Checking for a stuck previous cilium release..."
                  if helm status cilium -n kube-system >/tmp/cilium-status.txt 2>/dev/null; then
                    existing_status=$(awk '/^STATUS:/ {print $2}' /tmp/cilium-status.txt)
                    if [ "$existing_status" != "deployed" ]; then
                      echo "Existing cilium release is in state '$existing_status', not 'deployed' — resolve manually (e.g. helm uninstall cilium -n kube-system) before retrying" >&2
                      exit 1
                    fi
                  fi

                  echo "Preflight checks passed."
                command:
                - /bin/sh
                - -c
                env:
                - name: KUBECONFIG
                  value: /etc/kubeconfig/kubeconfig
                - name: EXPECTED_CONTROL_PLANE_COUNT
                  value: "3"
                image: alpine/k8s:1.32.13
                name: preflight
                volumeMounts:
                - mountPath: /etc/kubeconfig
                  name: kubeconfig-in-cluster
                  readOnly: true
              nodeSelector:
                node-role.kubernetes.io/control-plane: ""
              restartPolicy: Never
              serviceAccountName: cilium-installer
              tolerations:
              - effect: NoSchedule
                key: node.kubernetes.io/not-ready
                operator: Exists
              - effect: NoExecute
                key: node.kubernetes.io/not-ready
                operator: Exists
              - effect: NoExecute
                key: node.kubernetes.io/unreachable
                operator: Exists
              - effect: NoSchedule
                key: node-role.kubernetes.io/control-plane
                operator: Exists
              volumes:
              - configMap:
                  name: cilium-values-hf922bb52h
                name: values
              - configMap:
                  name: kubeconfig-in-cluster
                name: kubeconfig-in-cluster
          ttlSecondsAfterFinished: 1800
    - name: flux-operator-bootstrap
      contents: |
        apiVersion: v1
        kind: Namespace
        metadata:
          name: flux-system
        ---
        apiVersion: v1
        kind: ServiceAccount
        metadata:
          name: flux-operator-installer
          namespace: flux-system
        ---
        apiVersion: v1
        kind: ServiceAccount
        metadata:
          name: fluxinstance-apply
          namespace: flux-system
        ---
        apiVersion: v1
        kind: ServiceAccount
        metadata:
          name: sops-decrypt
          namespace: flux-system
        ---
        apiVersion: rbac.authorization.k8s.io/v1
        kind: Role
        metadata:
          name: fluxinstance-apply
          namespace: flux-system
        rules:
        - apiGroups:
          - fluxcd.controlplane.io
          resources:
          - fluxinstances
          verbs:
          - get
          - create
          - update
          - patch
        ---
        apiVersion: rbac.authorization.k8s.io/v1
        kind: Role
        metadata:
          name: sops-decrypt
          namespace: flux-system
        rules:
        - apiGroups:
          - ""
          resources:
          - secrets
          verbs:
          - get
          - create
          - update
          - patch
        ---
        apiVersion: rbac.authorization.k8s.io/v1
        kind: ClusterRole
        metadata:
          name: fluxinstance-apply-crd-check
        rules:
        - apiGroups:
          - apiextensions.k8s.io
          resources:
          - customresourcedefinitions
          verbs:
          - get
          - list
        ---
        apiVersion: rbac.authorization.k8s.io/v1
        kind: RoleBinding
        metadata:
          name: fluxinstance-apply
          namespace: flux-system
        roleRef:
          apiGroup: rbac.authorization.k8s.io
          kind: Role
          name: fluxinstance-apply
        subjects:
        - kind: ServiceAccount
          name: fluxinstance-apply
          namespace: flux-system
        ---
        apiVersion: rbac.authorization.k8s.io/v1
        kind: RoleBinding
        metadata:
          name: sops-decrypt
          namespace: flux-system
        roleRef:
          apiGroup: rbac.authorization.k8s.io
          kind: Role
          name: sops-decrypt
        subjects:
        - kind: ServiceAccount
          name: sops-decrypt
          namespace: flux-system
        ---
        apiVersion: rbac.authorization.k8s.io/v1
        kind: ClusterRoleBinding
        metadata:
          name: flux-operator-installer
        roleRef:
          apiGroup: rbac.authorization.k8s.io
          kind: ClusterRole
          name: cluster-admin
        subjects:
        - kind: ServiceAccount
          name: flux-operator-installer
          namespace: flux-system
        ---
        apiVersion: rbac.authorization.k8s.io/v1
        kind: ClusterRoleBinding
        metadata:
          name: fluxinstance-apply-crd-check
        roleRef:
          apiGroup: rbac.authorization.k8s.io
          kind: ClusterRole
          name: fluxinstance-apply-crd-check
        subjects:
        - kind: ServiceAccount
          name: fluxinstance-apply
          namespace: flux-system
        ---
        apiVersion: v1
        data:
          instance.yaml: |
            # Applied automatically by flux-operator/instance-apply-job.yaml, which
            # polls for the FluxInstance CRD (installed by job.yaml's Helm chart)
            # before applying this. Manual re-trigger/debug: kubectl apply -f
            # flux-operator/instance.yaml (once the CRD and the flux-system/git
            # secret both exist).
            apiVersion: fluxcd.controlplane.io/v1
            kind: FluxInstance
            metadata:
              name: flux
              namespace: flux-system
            spec:
              distribution:
                version: "2.x"
                registry: "ghcr.io/fluxcd"
              components:
                - source-controller
                - kustomize-controller
                - helm-controller
                - notification-controller
              cluster:
                type: kubernetes
                multitenant: false
                networkPolicy: true
              sync:
                kind: GitRepository
                url: "https://github.com/Jubblin/stormcraft.git"
                ref: "refs/heads/main"
                path: "clusters/stormcraft"
                pullSecret: "git"
        kind: ConfigMap
        metadata:
          name: fluxinstance-manifest-bk52h9mkg9
          namespace: flux-system
        ---
        apiVersion: v1
        data:
          git-secret.enc.yaml: |
            # PLACEHOLDER — not real ciphertext. Replace before this design works.
            #
            # Generate your age keypair and encrypt the real secret:
            #   container run --rm alpine:3.20 sh -c 'apk add --no-cache age >/dev/null && age-keygen' > age.agekey
            #   # copy the "public key:" line into .sops.yaml, then:
            #   cat <<'SECRET' > /tmp/git-secret.yaml
            #   apiVersion: v1
            #   kind: Secret
            #   metadata:
            #     name: git
            #   type: Opaque
            #   stringData:
            #     username: git
            #     password: <your GitHub PAT>
            #   SECRET
            #   container run --rm -v "$PWD:/work" -v /tmp/git-secret.yaml:/tmp/git-secret.yaml \
            #     -w /work alpine:3.20 sh -c \
            #     'apk add --no-cache sops >/dev/null && sops -e /tmp/git-secret.yaml' \
            #     > flux-operator/git-secret.enc.yaml
            #   rm /tmp/git-secret.yaml age.agekey  # move age.agekey to your password manager first
            apiVersion: v1
            kind: Secret
            metadata:
              name: git
            type: Opaque
            stringData:
              username: REPLACE_ME
              password: REPLACE_ME
        kind: ConfigMap
        metadata:
          name: git-secret-encrypted-gf267kc7gm
          namespace: flux-system
        ---
        apiVersion: v1
        data:
          retry-until.sh: |
            #!/bin/sh
            # Shared by both bootstrap Jobs (secret-decrypt-job.yaml, instance-apply-job.yaml).
            # Polls a check command until it succeeds, then runs an action command once.
            # Bounded by activeDeadlineSeconds on the Job itself, not by this script —
            # Kubernetes kills the pod (Job goes Failed) if the deadline passes while
            # this loop is still polling.
            set -eu

            CHECK="$1"
            ACTION="$2"

            until eval "$CHECK"; do
              echo "waiting: $CHECK"
              sleep 10
            done

            eval "$ACTION"
        kind: ConfigMap
        metadata:
          name: retry-until-script-k29726k56b
          namespace: flux-system
        ---
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: flux-operator-install
          namespace: flux-system
        spec:
          backoffLimit: 3
          template:
            spec:
              containers:
              - args:
                - |
                  set -eu
                  helm upgrade --install flux-operator \
                    oci://ghcr.io/controlplaneio-fluxcd/charts/flux-operator \
                    --namespace flux-system \
                    --wait --timeout 10m
                command:
                - /bin/sh
                - -c
                image: alpine/k8s:1.32.13
                name: install
              restartPolicy: Never
              serviceAccountName: flux-operator-installer
          ttlSecondsAfterFinished: 1800
        ---
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: fluxinstance-apply
          namespace: flux-system
        spec:
          activeDeadlineSeconds: 1800
          backoffLimit: 3
          template:
            spec:
              containers:
              - args:
                - kubectl get crd fluxinstances.fluxcd.controlplane.io >/dev/null 2>&1
                - kubectl apply -f /manifest/instance.yaml
                command:
                - /bin/sh
                - /scripts/retry-until.sh
                image: alpine/k8s:1.32.13
                name: apply
                volumeMounts:
                - mountPath: /scripts
                  name: retry-until-script
                - mountPath: /manifest
                  name: fluxinstance-manifest
              restartPolicy: Never
              serviceAccountName: fluxinstance-apply
              volumes:
              - configMap:
                  defaultMode: 365
                  name: retry-until-script-k29726k56b
                name: retry-until-script
              - configMap:
                  name: fluxinstance-manifest-bk52h9mkg9
                name: fluxinstance-manifest
          ttlSecondsAfterFinished: 1800
        ---
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: sops-decrypt-git-secret
          namespace: flux-system
        spec:
          activeDeadlineSeconds: 900
          backoffLimit: 3
          template:
            spec:
              containers:
              - args:
                - |
                  set -eu
                  apk add --no-cache sops age >/dev/null
                  SOPS_AGE_KEY_FILE=/age/age.agekey \
                    sops -d /encrypted/git-secret.enc.yaml | kubectl apply -f -
                command:
                - /bin/sh
                - -c
                image: alpine/k8s:1.32.13
                name: decrypt
                volumeMounts:
                - mountPath: /encrypted
                  name: git-secret-encrypted
                - mountPath: /age
                  name: sops-age
                  readOnly: true
              restartPolicy: Never
              serviceAccountName: sops-decrypt
              volumes:
              - configMap:
                  name: git-secret-encrypted-gf267kc7gm
                name: git-secret-encrypted
              - name: sops-age
                secret:
                  secretName: sops-age
          ttlSecondsAfterFinished: 1800
